import { ComunicadosPipe } from './comunicados.pipe';

describe('ComunicadosPipe', () => {
  it('create an instance', () => {
    const pipe = new ComunicadosPipe();
    expect(pipe).toBeTruthy();
  });
});
